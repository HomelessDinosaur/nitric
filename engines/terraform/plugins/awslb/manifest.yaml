name: nitric-aws-lb
version: 0.0.1
type: infra
deployment:
  # NOTE: // is required to reference a sub-path of repository for terraform modules
  terraform: github.com/HomelessDinosaur/nitric//engines/terraform/plugins/awslb/module?depth=1&ref=gcp-cloud-test

inputs:

outputs:
